version: '3.9'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./generated:/app/generated
    env_file:
      - .env
    restart: unless-stopped
